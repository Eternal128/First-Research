# ---------------------------------------------------------------------------
# Core runtime. Pinned to lower bounds that are known to expose the APIs used
# in src/pcc; upper bounds are deliberately omitted so the lockfile
# (requirements.lock, produced by `pip freeze`) is the reproducibility anchor.
# ---------------------------------------------------------------------------
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
pyyaml>=6.0

# ---------------------------------------------------------------------------
# Optional. Nothing in src/pcc imports these at module scope; every call site
# guards the import and degrades to a documented fallback. Install only the
# ones a given experiment needs.
# ---------------------------------------------------------------------------
# kloppy>=3.15          # provider-agnostic tracking/event loading (Metrica, SkillCorner, StatsBomb)
# statsbombpy>=1.11     # StatsBomb Open Data client
# socceraction>=1.5     # VAEP reference implementation, used only in the downstream study
# mplsoccer>=1.2        # pitch plotting
# statsmodels>=0.14     # cluster-robust GLM inference
# lightgbm>=4.0         # alternative GBM; sklearn HistGradientBoosting is the default
# pytest>=7.4           # test suite
